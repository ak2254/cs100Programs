class Course_Section:
    def __init__(self, secNum, Sname):
        self.secNum = secNum
        self.Sname = Sname
        lst = []
        self.enrolled = lst
    def enroll((name, ucid)):
        if tupes not in self.enrolled_students:
            self.enrolled_students.append(tuples)
            print(self.name + " with ucid " + self.ucid + " has successfully enrolled in section " + str(self.secNum) + "of" + self.Sname)
        else:
            print("already enrolled!!")
          

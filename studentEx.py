import student
for i in range(2):
    student.
    self.name = input("name: ")
    self.id = input( "UCID: ")
    self.gpa = input("GPA: ")
    if student.gradWithHonors() == True:
        student.displayInfo()
def makeStdList(filename):
    read = open(filename, 'r')
    lst = []
    for line in read:
        lst.append(line)
    return lst

def honorRoll():
    for lst in makeStdList():
        if self.gpa >= 3.8:
            
    
            
        
        

 
